<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ProyectoReservaCanchasMAUI.Views.AdministradoresPage"
             Title="AdministradoresPage">
    <StackLayout Padding="10">
        <CollectionView x:Name="AdministradoresList">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame BorderColor="Gray" Margin="5" Padding="10">
                        <Frame.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnAdministradorTapped" />
                        </Frame.GestureRecognizers>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <!-- Izquierda: Nombre y Correo -->
                            <StackLayout Grid.Column="0" Spacing="4">
                                <Label Text="{Binding Nombre}" FontAttributes="Bold" FontSize="Medium" />
                                <Label Text="{Binding Correo}" FontSize="Small" TextColor="Gray" />
                            </StackLayout>
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </StackLayout>
</ContentPage>
    