<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:ProyectoReservaCanchasMAUI.ViewModels"
             x:Class="ProyectoReservaCanchasMAUI.Views.AdministradoresPage"
             Title="Administrador">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="15">

            <Entry Placeholder="Banner ID" 
                   Keyboard="Numeric"
                   Text="{Binding NuevoAdministrador.BannerId}" />

            <Entry Placeholder="Nombre"
                   Text="{Binding NuevoAdministrador.Nombre}" />

            <Entry Placeholder="Correo"
                   Text="{Binding NuevoAdministrador.Correo}" />

            <Entry Placeholder="Contraseña"
                   IsPassword="True"
                   Text="{Binding NuevoAdministrador.Password}" />

            <Entry Placeholder="Teléfono"
                   Keyboard="Telephone"
                   Text="{Binding NuevoAdministrador.Telefono}" />

            <Entry Placeholder="Dirección"
                   Text="{Binding NuevoAdministrador.Direccion}" />

            <DatePicker Date="{Binding NuevoAdministrador.FechaNacimiento}" />

            <Label Text="Facultad" />
            <Picker ItemsSource="{Binding ListaFacultades}"
                    ItemDisplayBinding="{Binding Nombre}"
                    SelectedItem="{Binding FacultadSeleccionada}" />

            <Button Text="Guardar" Command="{Binding GuardarCommand}" />
            <Button Text="Cargar" Command="{Binding CargarCommand}" />
            <Button Text="Eliminar" Command="{Binding EliminarCommand}" />

            <Label Text="Lista de Administradores" FontAttributes="Bold" />
            <CollectionView ItemsSource="{Binding ListaAdministradores}"
                            SelectionMode="Single"
                            SelectedItem="{Binding AdministradorSeleccionado, Mode=TwoWay}"
                            HeightRequest="500">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame Padding="10" Margin="5" HasShadow="True" CornerRadius="5">
                            <VerticalStackLayout>
                                <Label Text="{Binding Nombre}" FontAttributes="Bold" />
                                <Label Text="{Binding Correo}" />
                                <Label Text="{Binding Telefono}" />
                                <Label Text="{Binding Facultad.Nombre}" FontSize="12" TextColor="Gray" />
                            </VerticalStackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>



